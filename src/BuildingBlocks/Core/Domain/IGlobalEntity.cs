namespace EDV.Framework.Core.Domain;

/// <summary>
/// Интерфейс-маркер — сущности, которые намеренно ОТКАЗЫВАЮТСЯ от автоматической
/// изоляции по арендатору. Любая сущность в контексте, производном от
/// <c>MultiTenantDbContext</c>, которая НЕ реализует этот интерфейс, автоматически
/// помечается <c>IsMultiTenant()</c> методом <c>BaseDbContext.OnModelCreating</c>.
///
/// Реализуйте этот интерфейс только для записей, которые действительно являются
/// общими для всех арендаторов — например, тарифные планы платформы,
/// кросс-арендаторные записи аудита/имперсонации, системные каталоги.
/// По умолчанию сущности изолируются по арендатору.
/// </summary>
public interface IGlobalEntity { }